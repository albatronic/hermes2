{#
    GconSecciones/arbol.html.twig
#}
<ul class="arbol">
{% for keyPadre,padre in arbol %}
    <li class="arbol">
        {% include 'GconSecciones/' ~ subtemplate with {'key':keyPadre,'item':padre} %}              
    </li>
    {% include 'GconSecciones/arbol.html.twig' with {'arbol': padre.hijos, 'subtemplate':'arbolEditar.html.twig'} %}    
    {#
    <ul class="arbol">
        {% for keyHijo,hijo in padre.hijos %}
            <li class="arbol">
                {% include 'GconSecciones/' ~ subtemplate with {'key':keyHijo,'item':hijo, 'nHermanos': padre.nHijos} %}
            </li>
            <ul class="arbol">
                {% for keyNieto,nieto in hijo.hijos %}
                    <li class="arbol">
                        {% include 'GconSecciones/' ~ subtemplate with {'key':keyNieto,'item':nieto, 'nHermanos': hijo.nHijos} %}
                    </li>
                {% endfor %}
            </ul>                        
        {% endfor %}
    </ul>
    #}
{% endfor %}
</ul>
